#include <stdio.h>

int bcnn(int a, int b) {
	if (b == 0) {
		return a;
	} else {
		b
	}
}


int main() {
	int a, b; scanf("%ld %d", &a, &b);
	int a0 = a, b0 = b;
	int ucln, r; 
	long long bcnn = a0 * b0 / ucln;

	if (b == 0) {
		ucln = a;
	} else {
		while (a % b != 0) {
			r = a % b;
			a = b;
			b = r;
		}

		ucln = b;
	}

	printf("%d\n%lld", ucln, bcnn);

	return 0;
}